Let's continue improving gofer (state in memory: gofer-rag-architecture —
read it first; latest commits 1e34826/74d54a0/8b4191b: near-title matching +
judged answer eval, sixth session 2026-07-14).

All evals green and re-verified after near-title matching (GPU box): retrieval
10/10, paraphrase 20-21/22 (min 20), realistic 29/33 (min 27, was 28), coverage
100%/97.6%, e2e 98/100 (same two ceiling misses), llm-gate 2/2. NEW: eval:answers
baseline — graded 24/30, grounded 24/24, answers 23/24, cited 23/24 (no min yet,
not in test:evals). Evals need the llama server (docker start llama-turboquant) —
it is a SHARED instance: poll localhost:8080/slots until is_processing:false
before server-heavy runs, and grep eval output for "query expansion unavailable"
before blaming the pipeline (an 83k-token neighbor prompt starved expansion and
sank a chain run to paraphrase 18/22; solo re-runs passed).

Priorities:
1. Answer-eval follow-ups: rerun eval:answers 2-3x on an idle server to measure
   judge run-to-run stability, then set a min and decide where it lives. Decide
   the blurry-sprite case (context covers downscale filtering, question asks
   upscale — retrieval nuance vs honest answers=false).
2. Expansion-starvation hardening: measure timeout rate first, then one retry on
   expandQuery's 15s timeout + a per-run timeout COUNT in eval output (the
   once-per-process warning masks how many questions silently lost expansion).
3. Expansion latency overlap (only if free, measure with diag-timing.ts first).

Closed — don't reopen: prompt levers for expansion vocabulary (two roulette
verdicts); spell-correct and expansion-term→title grounding (both measured
dead); the 3 remaining realistic misses have zero lexical overlap with their
answer titles (29-30/33 is honest); e2e Curve + Tree.font (98/100 ceiling).

Caveats: expansion nondeterminism comes in epochs (probe 3-4x); realistic noisy
±2 (28-30 = "still 29"); e2e ±1 (97-99 = "still 98"); confirm threshold/rank
conclusions on BOTH rerank backends; when eval-realistic fails, READ THE KEPT
CHAPTERS before blaming the pipeline.
