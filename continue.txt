Let's continue improving gofer (state documented in memory under
gofer-rag-architecture; latest work: realistic-questions eval + expansion
prompt fix for Godot-3-named questions, fifth session 2026-07-14).

Current state (all measured, GPU box): eval-retrieval 10/10, eval-paraphrase
22/22 (21 normal, RERANK_MIN 20), eval-realistic 28/33 (min 27, NEW — in
test:evals now), eval-coverage 100% / --lowercase 97.6%, coverage --rerank
100 e2e 98/100, llm-gate 2/2. `bun run test` wraps the whole chain in the
GPU rerank box (~6 min; realistic's 3 offtopic cases call the 27B via
query()). Evals need the llama server (docker start llama-turboquant).

Closed — don't reopen without new evidence:
- PROMPT LEVERS ARE EXHAUSTED for expansion vocabulary. The 4 realistic
  misses (falls-through-floor, freezes-on-load, andriod typo, world-size)
  all need docs vocabulary the 27B doesn't emit ("Continuous Collision
  Detection", "Background loading", "Exporting for Android", "Large world
  coordinates"). A "terms may be docs page titles" clause was probed with 3
  example pairs — every pair fixed some and collapsed others (yield→NONE,
  mouse/chase losing RayCast3D/NavigationAgent2D). That's the second
  prompt-roulette verdict after the fourth session's six wordings. Next
  lever must be NON-PROMPT (see candidates).
- e2e misses Curve + Tree.font: out of title-pin reach (diag-miss-shape.ts
  has numbers); 98/100 is this design's honest ceiling.
- Realistic-eval expects: when it fails, READ THE KEPT CHAPTERS first —
  3 of the first-run "failures" were the eval's own too-narrow regexes.

Next candidates, in rough priority — measure, don't guess:

1. Ground expansion terms in the real chapter list: after expandQuery,
   fuzzy-match each term against the 1581 chapter titles (the list is
   cached in db.ts); a matched term becomes a verbatim title → titleSearch
   fires on it. Could fix andriod ("Android"→? probably not) and world-size
   (no — "FloatingPointPrecision" matches nothing). Honestly evaluate
   whether it fixes ANY of the 4 before building; it may only help future
   cases. The stronger variant: give titleSearch's within-chapter treatment
   to NEAR-title terms.
2. Question spell-correct against corpus vocabulary (symspell-style over
   FTS tokens): "andriod"→"android", "colison"→"collision", "raycst2d"→
   "raycast2d". Fixes the typo category at the pool level; measure BM25
   after correction actually surfaces "Exporting for Android" (many
   Android chapters compete).
3. Expansion latency polish (only if free): ~0.5s per title-less query;
   overlap expandQuery with the unexpanded embed only after measuring
   (scripts/diag-timing.ts).
4. Answer quality is still unmeasured beyond the refusal gate — nobody
   reads the LLM's answers systematically. A judged answer eval (grounding
   + correctness on the realistic set) is the next fresh axis.

Measurement caveats (all bit or nearly bit earlier sessions):
- Expansion nondeterminism comes in EPOCHS: today 4/4 identical per
  question all day, other days it flips run to run. Probe 3-4x; realistic
  has 2 flaky cases (health-bar, null-instance) → min 27 has that margin.
- e2e --rerank 100 noisy ±1 (97-99 = "still 98"); realistic noisy ±2
  (27-29 = "still 28"). Only investigate bigger drops.
- Eval box GGUF Q8_0 vs serving ONNX q8 logits differ (flips near -4 and
  rank-5-vs-6); confirm pin/threshold conclusions on BOTH backends
  (diag-miss-shape.ts runs either).
- eval-coverage's e2e sample shares the seeded RNG with the member sample:
  identical flags or numbers aren't comparable.
- bun run lint: prettier only covers src/ and ingest/ — scripts/ gets
  eslint+tsc but no formatting.

Notes: retrieve()/retrieveDetailed() test without the answer LLM; bun run
scripts/ask.ts "..." is the full pipeline. Reranker: read the RAW logit;
embedder stays fp16 (ONNX q8 is a distorted space, don't "optimize"). bun
test fails with "0 test files" — pre-existing. Diagnostics:
diag-paraphrase-rank.ts, diag-gap.ts, diag-timing.ts, diag-miss-shape.ts.
Corpus text dump for expectation-verification greps: see the tmp-dump
pattern in this session's history or query LanceDB directly (chapter list
is cached in db.ts chapterList()).
